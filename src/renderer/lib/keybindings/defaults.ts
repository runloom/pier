import { DEFAULT_KEYMAP as SHARED_DEFAULT_KEYMAP } from "@shared/keybindings.ts";
import type { KeybindingInput } from "./types.ts";

export const DEFAULT_KEYMAP: readonly KeybindingInput[] = SHARED_DEFAULT_KEYMAP;
