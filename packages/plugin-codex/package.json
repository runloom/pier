{
  "name": "@pier/plugin-codex",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:main": "vite build --config vite.config.main.ts",
    "build:renderer": "vite build --config vite.config.renderer.ts",
    "dev:main": "vite build --config vite.config.main.ts --watch",
    "dev:renderer": "vite build --config vite.config.renderer.ts --watch",
    "build": "pnpm build:main && pnpm build:renderer",
    "package": "node ../../scripts/pack-plugin.mjs",
    "build:package": "pnpm build && pnpm package"
  },
  "dependencies": {
    "@pier/plugin-api": "workspace:*",
    "@pier/ui": "workspace:*",
    "write-file-atomic": "^8.0.0"
  },
  "peerDependencies": {
    "react": "19.2.7",
    "react-dom": "19.2.7"
  }
}
