export const DATA_SCHEMA_ID = "claude.accounts" as const;
export const WATCH_SUPPRESS_MS = 1500;
/** Browser OAuth login sessions expire after this long (stale PKCE state). */
export const LOGIN_SESSION_TTL_MS = 30 * 60 * 1000;
