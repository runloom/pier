{
  "schemaVersion": 1,
  "id": "design",
  "axis": "recipe",
  "title": "Design mockup",
  "description": "Multi-device product frames on a world stage. Comments stay in host Design Mode.",
  "kind": "composition",
  "stage": "world",
  "template": "templates/design-mockup.canvas.tsx",
  "primitives": ["WorldStage", "Layer", "Artboard"],
  "required": ["worldRoot", "deviceFrames"],
  "antiPatterns": [
    "ArtboardStage as the canvas root (that is a flow fit-all card)",
    "stacking phone and desktop frames as a document inside Frame",
    "hand-rolled annotation chrome; use host Design Mode pins",
    "DocsShell or document font on a mockup",
    "inventing methodology tabs for a mockup"
  ],
  "agentPrompt": "WorldStage root. Artboard preset + Layer x/y. UI font. Do not wrap in Frame. Do not fake comment pins."
}
