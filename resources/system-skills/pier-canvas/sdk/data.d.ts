/**
 * Product data for canvases is Host API (`import { host, useHostSnapshot }
 * from "pier/host"`), not KPI hooks. Bind commands, events, and snapshots
 * on the canvas and compose `Item` / `Table` / `DataChart` yourself.
 *
 * Adjacent files stay `useCanvasFile` from `./files.js`.
 */
export {};
