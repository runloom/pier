# 侧栏整改与验证记录

> 以下保留历史检查过程；最新审查结论、修复闭环与截图以 [AUDIT.md](AUDIT.md) 为准。早期“尚未完成”与旧布局记录不代表最终稿状态。

日期：2026-09-09。入口：`workbench-sidebar-review.canvas.tsx`。只覆盖侧栏交互设计，不覆盖真实工作树切换或宿主生产接线。

## 已关闭的源码问题

| 问题 | 修复 | 证据边界 |
| --- | --- | --- |
| Button 基础外观被二次覆盖 | 对象行改为 Item 单一外观；工具 Button 使用 muted tone；删除正常字体、hover、焦点与重复按钮尺寸覆盖 | 独立源码复核；28px Item 适配仍是本 Canvas 的布局约束 |
| 局部自行混色 | 删除 color-mix；attention / selected 映射既有语义令牌 | 源码扫描；未宣称所有主题对比度已实测 |
| Progress 类型绕过 | 删除展开对象传 value；保留可访问状态文字、42% 数字与完成图标 | 公共 SDK 类型检查；SDK 声明缺口本身尚未修复 |
| 反馈残留 | 选择与定位共用反馈入口，折叠和打开浮层清除旧反馈 | 实际宿主连续点击 + 组件回归 |
| 场景无入口 | 单一画板选项接通四场景、四语言、三宽度及两高度 | 完整 Canvas 入口组件回归；实际宿主矩阵尚未完整完成 |
| 条件类模板 | 改为静态 class 与 data 属性 | 源码扫描、Biome 显式检查 |
| 当前会话没有视觉消费 | data-located 映射已有选中背景；切工作树清除旧定位，跨窗保留本窗定位 | 源码复核 + 组件状态回归；最终持久高亮宿主复验仍待完成 |
| Select 条目缺组 | 所有 SelectContent → SelectGroup → SelectItem | 独立复核发现后修正 |
| 远程身份槽重复 | 远程示例改用同屏空闲的第 6 槽 | 示例数据与组件断言 |
| 名称长度猜布局 | 移除 name.length 阈值，按 Canvas 可用宽度折叠摘要 | 源码检查；三宽度视觉矩阵仍待完成 |

## 本次实际完成的检查

- 公开 SDK TypeScript 检查：单独映射 `resources/system-skills/pier-canvas/sdk`，没有借用仓库真实组件类型掩盖公开声明差异。
- Live Modules：使用现有 compileLiveModule 探针，默认依赖围栏编译 `ok=true`，无警告、无未处理 glob。
- Biome：逐文件经 stdin 指定文件名检查；根配置排除 Canvas，所以没有把“检查 0 文件”当通过。
- 组件交互：通过真实 `pierCanvasExports` 渲染，11 项通过；使用 jsdom，明确不验证像素布局。测试覆盖反馈替换、本窗/跨窗选择、未读变化、方向键与折叠、更新阶段关闭后保留、默认不显示本机目录、远程身份槽、空态返回、四语标签与摘要，以及完整 Canvas 入口的宽度/高度/场景选择。
- 实际安装的 `/Applications/Pier.app`：当前设计成功挂载；已观察默认深色 256px 侧栏与底部入口，设置 → 工作树 → 会话的反馈按顺序更新，向下键从 Claude 移到 Gemini。未出现 Canvas 编译/崩溃错误区域；这不等于检查过全部 console。

本次编译、类型与组件测试临时工具位于 `/tmp/pier-canvas-viability/`，不作为 Canvas 运行依赖，也未创建外部预览服务。

## 尚未通过完整宿主验收的项目

实际宿主后续操作多次被前台窗口切换中断，以下项目没有标为通过：

- 深浅主题 × 224 / 256 / 320px 的完整视觉矩阵、560px 高度下的滚动与底部固定。
- 100% 与缩放视口下的精确行高、图标尺寸、焦点裁切和 Popover 位置；标题与画板的最终间距。
- Escape 关闭浮层后的实际焦点返回、全部语言的视觉截断和读屏播报。
- 最新身份令牌在实际宿主中的六色区分。旧宿主缺令牌时现使用项目身份色兼容资产；当前深色默认场景已实际显示五种不同色块，第六种及浅色值经源码对照校验，完整宿主主题矩阵仍待完成。

复验顺序：在 Pier 打开入口 → 画板选项选择尺寸/场景/语言 → 设置、工作树、本窗会话、跨窗会话连续点击 → 上下/左右键与 Tab → 更新下载演示并关闭/重开浮层 → 在宿主切换深浅主题并恢复原偏好。需要同时检查可见状态、焦点和反馈，不能只看截图。


## 2026-09-09 三处细节反馈修正

- 底部移除 Separator 与其导入，没有追加 border-top；原有留白承担分区。
- 添加按钮改用项目 icon-xs 的 24px 外观与 14px 图标；透明伪元素将命中范围扩展到 28px，避免只靠缩小点击目标来缩小 hover。
- 身份色不再在旧宿主全部回退灰色。新增仅含 12 个身份色值的生成 JSON，来源为 globals.css；宿主 token 优先，仅色块私有 fallback 消费快照。未修改宿主主题、SDK 或其他产品代码。
- 新资产的 12/12 值及 token SHA-256 与源一致，两主题各六个值互不相同。Live Modules 编译和公开 SDK 类型检查通过。
- 实际安装 Pier 深色 256px：已观察分割线消失、默认五个工作树显示不同身份色；添加按钮显现后点击只产生“在 pier 新建工作树”反馈，项目保持展开。悬停/焦点外观已观察，28px 透明命中扩展尚未做实际指针边界测量。

## 2026-09-09 更新入口缺省态修正

- 依据当前 `appUpdateNeedsAttention`，无更新时不渲染入口；默认画板也展示无更新。设置 / 通知 / 条件更新统一靠左、间距 6px，无空位、无 `space-between`。
- 画板选项新增四态选择，与浮层阶段演示共享状态；切换四态不重置工作树选择或替换常驻按钮。待重启使用安装版 Lucide 的 RotateCw 图标和四语重启动作文案。
- 显式 Biome、Live Modules 编译、公开 SDK 类型检查通过。11 项交互回归通过；新增入口四态往返检查、设置 / 通知节点保持和工作树选择保留检查。jsdom 不作为像素位移证据。
- 实际 Pier 重新加载后已观察默认更新入口消失，底部只保留靠左的设置 / 通知；尝试打开画板选项时原生自动化返回 `elementHasNoFrame` / `noWindowsAvailable`，因此本轮未把其他三态的宿主视觉位置标为已实测；四态切换与节点保留已经由实际 Canvas 入口组件测试覆盖。

## 2026-09-09 底部视觉重心修正

- 上一节的全部靠左排列由本次反馈替代：左侧设置增加可见文字，通知锚定右侧，更新按需插在通知左侧。DOM 与 Tab 都按设置 → 更新 → 通知排列，不用 CSS order 或绝对定位。
- 底部水平内缩和设置按钮水平内边距均为 8px，标准交互高度保持 28px；设置允许收缩，避免窄栏进度挤出通知。下载进度也纳入四语按钮可访问名称。
- 最新 Live Modules、公开 SDK 类型、显式 Biome 及 11 项交互检查通过。实际 Pier 深色 256px 已观察左侧「齿轮 + 设置」与右侧通知的新布局。
- 窄栏英文下载态已做源码宽度风险审查并增加空间余量；实际选项操作仍受到 native `elementHasNoFrame` / `noWindowsAvailable` 阻断，未声明 224px 英文或全部更新状态的像素测量通过。
