diff --git a/dist/model/FileTreeController.js b/dist/model/FileTreeController.js
index efa8b6a2fd70e0d1f1dec1d90ee87268c72f56c4..aaf3ae8243aefce61461368d68afd542528a0802 100644
--- a/dist/model/FileTreeController.js
+++ b/dist/model/FileTreeController.js
@@ -200,6 +200,7 @@ var FileTreeController = class FileTreeController {
 		this.#scrollRequest = {
 			id: this.#scrollRequestId += 1,
 			offset: normalizeScrollOffset(options?.offset),
+			performScroll: options?.performScroll,
 			visibleIndex: targetIndex
 		};
 		this.#emit();
@@ -504,13 +505,15 @@ var FileTreeController = class FileTreeController {
 			return false;
 		}
 		try {
-			if (dropPlan.operations.length === 1) {
-				const singleOperation = dropPlan.operations[0];
-				if (singleOperation == null || singleOperation.type !== "move") throw new Error("Expected a single move operation for one-item drops");
-				this.#store.move(singleOperation.from, singleOperation.to, { collision: singleOperation.collision });
-			} else {
-				this.#validateBatchDropOperations(dropPlan.operations);
-				this.#store.batch(dropPlan.operations);
+			if (this.#dragAndDropConfig?.commitOnDrop !== false) {
+				if (dropPlan.operations.length === 1) {
+					const singleOperation = dropPlan.operations[0];
+					if (singleOperation == null || singleOperation.type !== "move") throw new Error("Expected a single move operation for one-item drops");
+					this.#store.move(singleOperation.from, singleOperation.to, { collision: singleOperation.collision });
+				} else {
+					this.#validateBatchDropOperations(dropPlan.operations);
+					this.#store.batch(dropPlan.operations);
+				}
 			}
 		} catch (error) {
 			this.#emit();
@@ -518,6 +521,7 @@ var FileTreeController = class FileTreeController {
 			return false;
 		}
 		this.#dragAndDropConfig?.onDropComplete?.(dropPlan.result);
+		this.#emit();
 		return true;
 	}
 	subscribe(listener) {
@@ -943,7 +947,7 @@ var FileTreeController = class FileTreeController {
 		}
 		const currentVisiblePaths = this.#projectionPaths;
 		this.#searchMatchingPaths = currentVisiblePaths.filter((path) => this.#searchMatchPathSet.has(path));
-		if (this.#searchMode !== "hide-non-matches" || this.#searchMatchPathSet.size === 0) {
+		if (this.#searchMode !== "hide-non-matches") {
 			this.#searchVisibleIndices = null;
 			this.#searchVisiblePaths = null;
 			this.#searchVisibleIndexByPath = null;
@@ -1042,7 +1046,7 @@ var FileTreeController = class FileTreeController {
 			focusCandidate ??= path;
 		}
 		this.#searchMatchPathSet = matchingPathSet;
-		const searchVisiblePathSet = this.#searchMode === "hide-non-matches" && matchingPaths.length > 0 ? /* @__PURE__ */ new Set() : null;
+		const searchVisiblePathSet = this.#searchMode === "hide-non-matches" ? /* @__PURE__ */ new Set() : null;
 		this.#searchVisiblePathSet = searchVisiblePathSet;
 		const expandedPaths = this.#searchMode === "expand-matches" ? new Set(this.#searchPreviousExpandedPaths ?? []) : /* @__PURE__ */ new Set();
 		for (const matchingPath of matchingPaths) {
diff --git a/dist/model/internalTypes.d.ts b/dist/model/internalTypes.d.ts
index 5b0fdd5a3b483439ae256795f3dad5a32182ab78..b5377378cd295bacf11150ee0a4bbecda21a3cd2 100644
--- a/dist/model/internalTypes.d.ts
+++ b/dist/model/internalTypes.d.ts
@@ -1,6 +1,6 @@
 import { FileTreeIcons } from "../iconConfig.js";
 import { GitStatus } from "../publicTypes.js";
-import { FileTreeCompositionOptions, FileTreePublicId, FileTreeRenderOptions, FileTreeRowDecorationRenderer, FileTreeScrollOffset, FileTreeSearchBlurBehavior, FileTreeVisibleRow } from "./publicTypes.js";
+import { FileTreeCompositionOptions, FileTreePublicId, FileTreeRenderOptions, FileTreeRowDecorationRenderer, FileTreeScrollOffset, FileTreeScrollToPathOptions, FileTreeSearchBlurBehavior, FileTreeVisibleRow } from "./publicTypes.js";
 import { FileTreeController } from "./FileTreeController.js";
 
 //#region src/model/internalTypes.d.ts
@@ -29,6 +29,7 @@ interface FileTreeStickyWindowLayout {
 interface FileTreeScrollRequest {
   id: number;
   offset: FileTreeScrollOffset;
+  performScroll?: FileTreeScrollToPathOptions['performScroll'];
   visibleIndex: number;
 }
 interface FileTreeSlotHost {
diff --git a/dist/model/publicTypes.d.ts b/dist/model/publicTypes.d.ts
index 0d75f14780eb033fdbc8fde76e6f4c080e1d096a..d822c8a8fffb5485ab3ead0611f43b06e586f06d 100644
--- a/dist/model/publicTypes.d.ts
+++ b/dist/model/publicTypes.d.ts
@@ -42,6 +42,12 @@ interface FileTreeGitStatusPatch {
 }
 interface FileTreeStoreOptions {
   flattenEmptyDirectories?: boolean;
+  /**
+   * Minimum node depth that may start a single-child directory flatten chain.
+   * Synthetic root is depth 0; top-level paths are depth 1. Default 0.
+   * Set to 2 so top-level group roots stay unmerged while nested path chains still flatten.
+   */
+  flattenMinDepth?: number;
   initialExpansion?: FileTreeInitialExpansion;
   initialExpandedPaths?: readonly FileTreePublicId[];
   presorted?: boolean;
@@ -116,6 +122,8 @@ type FileTreeScrollOffset = 'top' | 'center' | 'nearest';
 interface FileTreeScrollToPathOptions {
   focus?: boolean;
   offset?: FileTreeScrollOffset;
+  /** Runs at the deferred DOM write. Call write synchronously, or skip to cancel. */
+  performScroll?: (element: HTMLElement, write: () => void) => void;
 }
 type FileTreeSearchMode = 'expand-matches' | 'collapse-non-matches' | 'hide-non-matches';
 type FileTreeSearchBlurBehavior = 'close' | 'retain';
@@ -146,6 +154,17 @@ interface FileTreeDropResult extends FileTreeDropContext {
 interface FileTreeDragAndDropConfig {
   canDrag?: (paths: readonly FileTreePublicId[]) => boolean;
   canDrop?: (event: FileTreeDropContext) => boolean;
+  /**
+   * When false, completeDrag does not mutate the path store; onDropComplete
+   * still fires. Default true (upstream).
+   */
+  commitOnDrop?: boolean;
+  /**
+   * Mouse pointer distance before starting an in-tree drag. 0 (default) keeps
+   * native HTML5 `draggable`. Values > 0 disable row `draggable` and use a
+   * pointer session (same machinery as touch).
+   */
+  pointerDragThresholdPx?: number;
   onDropComplete?: (event: FileTreeDropResult) => void;
   onDropError?: (error: string, event: FileTreeDropContext) => void;
   openOnDropDelay?: number;
diff --git a/dist/path-store/src/flatten.js b/dist/path-store/src/flatten.js
index 1f7a2fd6d24db92e4c42aa7d3b5a7881a36af7ae..df9ff1b014f8d1c93212727a86b86a236f24947b 100644
--- a/dist/path-store/src/flatten.js
+++ b/dist/path-store/src/flatten.js
@@ -1,9 +1,13 @@
-import { hasNodeFlag, isDirectoryNode } from "./internal-types.js";
+import { getNodeDepth, hasNodeFlag, isDirectoryNode } from "./internal-types.js";
 //#region ../path-store/src/flatten.ts
 function getFlattenedChildDirectoryId(state, directoryNodeId) {
 	if (state.snapshot.options.flattenEmptyDirectories !== true) return null;
 	const directoryNode = state.snapshot.nodes[directoryNodeId];
 	if (directoryNode == null || !isDirectoryNode(directoryNode) || hasNodeFlag(directoryNode, 2)) return null;
+	// Depth gate: refuse to start/continue flatten from nodes shallower than flattenMinDepth.
+	// Default 0 preserves upstream behavior for all real path nodes (depth >= 1).
+	const flattenMinDepth = state.snapshot.options.flattenMinDepth ?? 0;
+	if (getNodeDepth(directoryNode) < flattenMinDepth) return null;
 	const directoryIndex = state.snapshot.directories.get(directoryNodeId);
 	if (directoryIndex == null || directoryIndex.childIds.length !== 1) return null;
 	const childId = directoryIndex.childIds[0];
diff --git a/dist/path-store/src/options.js b/dist/path-store/src/options.js
index 0a7e7778124558bd62770e65c1f0928bc61b4fcc..1f2153385e4c7b24b97a4ecc5ef74760e3841103 100644
--- a/dist/path-store/src/options.js
+++ b/dist/path-store/src/options.js
@@ -1,7 +1,15 @@
 //#region ../path-store/src/options.ts
 function resolvePathStoreOptions(options = {}) {
+	const flattenMinDepthRaw = options.flattenMinDepth;
+	const flattenMinDepth = typeof flattenMinDepthRaw === "number" && Number.isFinite(flattenMinDepthRaw)
+		? Math.max(0, flattenMinDepthRaw | 0)
+		: 0;
 	return {
 		flattenEmptyDirectories: options.flattenEmptyDirectories !== false,
+		// Minimum node depth eligible to start a single-child directory flatten chain.
+		// Root synthetic node is depth 0; top-level paths are depth 1.
+		// Use 2 to keep depth-1 group roots unmerged while still flattening path chains beneath them.
+		flattenMinDepth,
 		sort: options.sort ?? "default"
 	};
 }
diff --git a/dist/path-store/src/store.js b/dist/path-store/src/store.js
index d59ea08e0c72a2f3f5caf95dacc541b60c86d0e2..fa14e431b00a6554d3e1274d9dea3ac824800772 100644
--- a/dist/path-store/src/store.js
+++ b/dist/path-store/src/store.js
@@ -13,6 +13,7 @@ import { collapsePath, expandPath, getVisibleCount, getVisibleIndexByPath, getVi
 function initializeOpenVisibleCounts(state) {
 	const { directories, nodes, options, rootId, presortedDirectoryNodeIds } = state.snapshot;
 	const flattenEmptyDirectories = options.flattenEmptyDirectories === true;
+	const flattenMinDepth = options.flattenMinDepth ?? 0;
 	const walkDirectory = (nodeId) => {
 		const currentNode = nodes[nodeId];
 		if (currentNode == null || !isDirectoryNode(currentNode)) return;
@@ -34,7 +35,11 @@ function initializeOpenVisibleCounts(state) {
 		if (childCount >= 128) rebuildVisibleChildChunks(nodes, currentIndex);
 		currentNode.subtreeNodeCount = 1 + totalChildSubtreeNodeCount;
 		let newVisibleSubtreeCount;
-		if (flattenEmptyDirectories && childCount === 1) {
+		if (
+			flattenEmptyDirectories &&
+			childCount === 1 &&
+			getNodeDepth(currentNode) >= flattenMinDepth
+		) {
 			const onlyChild = nodes[childIds[0]];
 			newVisibleSubtreeCount = onlyChild != null && isDirectoryNode(onlyChild) ? totalChildVisibleSubtreeCount : 1 + totalChildVisibleSubtreeCount;
 		} else newVisibleSubtreeCount = 1 + totalChildVisibleSubtreeCount;
diff --git a/dist/preparedInput.d.ts b/dist/preparedInput.d.ts
index 92781bbe6f0c2ff49d5242a55dee736d3fff40b3..b41f1861047a2dc8d83a3bac8d52b664aab7770c 100644
--- a/dist/preparedInput.d.ts
+++ b/dist/preparedInput.d.ts
@@ -8,6 +8,7 @@ interface FileTreePreparedInput {
 }
 declare function prepareFileTreeInput(paths: readonly string[], options?: {
   flattenEmptyDirectories?: boolean;
+  flattenMinDepth?: number;
   sort?: 'default' | FileTreeSortComparator;
 }): FileTreePreparedInput;
 declare function preparePresortedFileTreeInput(paths: readonly string[]): FileTreePreparedInput;
diff --git a/dist/render/FileTree.d.ts b/dist/render/FileTree.d.ts
index b86b9865272df83225a6b0330117ddb9c83a8ee0..bfe7ac319b9376f182bac45b354c894e1951903b 100644
--- a/dist/render/FileTree.d.ts
+++ b/dist/render/FileTree.d.ts
@@ -20,6 +20,7 @@ declare class FileTree implements FileTreeMutationHandle, FileTreeSearchSessionH
   focusPath(path: string): void;
   scrollToPath(path: FileTreePublicId, options?: FileTreeScrollToPathOptions): void;
   focusNearestPath(path: string | null): string | null;
+  selectOnlyPath(path: string): void;
   add(path: string): void;
   batch(operations: readonly FileTreeBatchOperation[]): void;
   applyGitStatusPatch(patch: FileTreeGitStatusPatch): void;
diff --git a/dist/render/FileTree.js b/dist/render/FileTree.js
index a3acb5917de61ee8283f12fe5a73282437cfcfab..3b9a14bed67c25109e2303259e9911a6f0ea61ca 100644
--- a/dist/render/FileTree.js
+++ b/dist/render/FileTree.js
@@ -177,6 +177,9 @@ var FileTree = class {
 	focusNearestPath(path) {
 		return this.#controller.focusNearestPath(path);
 	}
+	selectOnlyPath(path) {
+		this.#controller.selectOnlyPath(path);
+	}
 	add(path) {
 		this.#controller.add(path);
 	}
diff --git a/dist/render/FileTreeView.js b/dist/render/FileTreeView.js
index 3d02080ad553347e7fe95e261c993b607b4134c2..f88e06c6c6f868353ce3425d9cb611b181bfb8c3 100644
--- a/dist/render/FileTreeView.js
+++ b/dist/render/FileTreeView.js
@@ -3,7 +3,7 @@ import { createFileTreeIconResolver } from "./iconResolver.js";
 import { FILE_TREE_DEFAULT_ITEM_HEIGHT } from "../model/virtualization.js";
 import { FILE_TREE_RENAME_VIEW } from "../model/FileTreeController.js";
 import { Icon } from "../components/Icon.js";
-import { MiddleTruncate, Truncate } from "../components/OverflowText.js";
+import { Truncate } from "../components/OverflowText.js";
 import { computeFileTreeLayout, computeStickyRows } from "../model/layout.js";
 import { GIT_STATUS_DESCENDANT_TITLE, GIT_STATUS_LABEL, GIT_STATUS_TITLE } from "../utils/gitStatusPresentation.js";
 import { shouldBumpControllerRevision } from "./controllerSnapshotSubscription.js";
@@ -21,17 +21,20 @@ function formatFlattenedSegments(row, renameInput = null, dragTargetFlattenedSeg
 	"use no memo";
 	const segments = row.flattenedSegments;
 	if (segments == null || segments.length === 0) return renameInput ?? row.name;
-	return /* @__PURE__ */ jsx("span", {
+	const content = /* @__PURE__ */ jsx("span", {
 		"data-item-flattened-subitems": true,
 		children: segments.map((segment, index) => {
 			const isLast = index === segments.length - 1;
 			return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("span", {
 				"data-item-flattened-subitem": segment.path,
 				"data-item-flattened-subitem-drag-target": dragTargetFlattenedSegmentPath === segment.path ? "true" : void 0,
-				children: isLast && renameInput != null ? renameInput : /* @__PURE__ */ jsx(Truncate, { children: segment.name })
+				children: isLast && renameInput != null ? renameInput : segment.name
 			}), index < segments.length - 1 ? " / " : ""] }, segment.path);
 		})
 	});
+	if (renameInput != null) return content;
+	// Keep per-segment hit targets; wrap whole chain in one Truncate for trailing ellipsis.
+	return /* @__PURE__ */ jsx(Truncate, { children: content });
 }
 function getFileTreeRowPath(row) {
 	return row.isFlattened ? row.flattenedSegments?.findLast((segment) => segment.isTerminal)?.path ?? row.path : row.path;
@@ -367,11 +370,7 @@ function renderFileTreeRowContent(row, resolveIcon, { actionLaneEnabled = false,
 		}),
 		/* @__PURE__ */ jsx("div", {
 			"data-item-section": "content",
-			children: row.isFlattened ? formatFlattenedSegments(row, renameInput, dragTargetFlattenedSegmentPath) : renameInput ?? /* @__PURE__ */ jsx(MiddleTruncate, {
-				minimumLength: 5,
-				split: "extension",
-				children: row.name
-			})
+			children: row.isFlattened ? formatFlattenedSegments(row, renameInput, dragTargetFlattenedSegmentPath) : renameInput ?? /* @__PURE__ */ jsx(Truncate, { children: row.name })
 		}),
 		decorationLaneEnabled ? /* @__PURE__ */ jsx("div", {
 			"data-item-section": "decoration",
@@ -392,7 +391,7 @@ function renderFileTreeRowContent(row, resolveIcon, { actionLaneEnabled = false,
 	] });
 }
 function renderStyledRow(frame, row, key, options = {}) {
-	const { controller, renameView, visualFocusPath, contextHoverPath, draggedPathSet, dragTarget, dragAndDropEnabled, shouldSuppressContextMenu, handleRowDragStart, handleRowDragEnd, handleRowTouchStart, instanceId, itemHeight, gitStatusByPath, ignoredGitDirectories, ignoredInheritanceCache, directoriesWithGitChanges, gitLaneActive, contextMenuEnabled, contextMenuTriggerMode, contextMenuButtonTriggerEnabled, contextMenuButtonVisibility, contextMenuRightClickEnabled, registerRenameInput, registerButton, resolveIcon, renderDecorationForRow, openContextMenuForRow, onRowClick, onKeyDown } = frame;
+	const { controller, renameView, visualFocusPath, contextHoverPath, draggedPathSet, dragTarget, dragAndDropEnabled, shouldSuppressContextMenu, handleRowDragStart, handleRowDragEnd, handleRowTouchStart, handleRowPointerDown, pointerDragThresholdPx, suppressClickRef, instanceId, itemHeight, gitStatusByPath, ignoredGitDirectories, ignoredInheritanceCache, directoriesWithGitChanges, gitLaneActive, contextMenuEnabled, contextMenuTriggerMode, contextMenuButtonTriggerEnabled, contextMenuButtonVisibility, contextMenuRightClickEnabled, registerRenameInput, registerButton, resolveIcon, renderDecorationForRow, openContextMenuForRow, onRowClick, onKeyDown } = frame;
 	const targetPath = getFileTreeRowPath(row);
 	const { isParked = false, mode = "flow", style } = options;
 	const isSticky = mode === "sticky";
@@ -483,9 +482,9 @@ function renderStyledRow(frame, row, key, options = {}) {
 	return /* @__PURE__ */ jsx("button", {
 		...commonProps,
 		type: "button",
-		draggable: dragAndDropEnabled && !isParked,
-		onDragEnd: dragAndDropEnabled && !isParked ? handleRowDragEnd : void 0,
-		onDragStart: dragAndDropEnabled && !isParked ? (event) => {
+		draggable: dragAndDropEnabled && !isParked && pointerDragThresholdPx <= 0,
+		onDragEnd: dragAndDropEnabled && !isParked && pointerDragThresholdPx <= 0 ? handleRowDragEnd : void 0,
+		onDragStart: dragAndDropEnabled && !isParked && pointerDragThresholdPx <= 0 ? (event) => {
 			handleRowDragStart(event, row, targetPath);
 		} : void 0,
 		onMouseDown: (event) => {
@@ -495,10 +494,17 @@ function renderStyledRow(frame, row, key, options = {}) {
 			}
 			if (controller.isSearchOpen()) event.preventDefault();
 		},
+		onPointerDown: dragAndDropEnabled && !isParked && pointerDragThresholdPx > 0 ? (event) => {
+			handleRowPointerDown(event, row, targetPath);
+		} : void 0,
 		onTouchStart: dragAndDropEnabled && !isParked ? (event) => {
 			handleRowTouchStart(event, row, targetPath);
 		} : void 0,
 		onClick: (event) => {
+			if (suppressClickRef.current) {
+				suppressClickRef.current = false;
+				return;
+			}
 			onRowClick(event, row, targetPath, mode);
 		},
 		children: rowContent
@@ -546,6 +552,11 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 	const touchSourceElementRef = useRef(null);
 	const touchStartPointRef = useRef(null);
 	const touchLongPressTimerRef = useRef(null);
+	const pointerCleanupRef = useRef(null);
+	const pointerDragActiveRef = useRef(false);
+	const pointerDragArmedRef = useRef(null);
+	const pointerPreviewOffsetRef = useRef(null);
+	const suppressClickRef = useRef(false);
 	const ignoredInheritanceCache = useMemo(() => /* @__PURE__ */ new Map(), []);
 	const [, setControllerRevision] = useState(0);
 	const hasSeenInitialControllerSnapshotRef = useRef(false);
@@ -655,6 +666,7 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 	const focusedIndex = controller.getFocusedIndex();
 	const scrollRequest = controller.getScrollRequest();
 	const dragAndDropEnabled = controller.isDragAndDropEnabled();
+	const pointerDragThresholdPx = controller.getDragAndDropConfig()?.pointerDragThresholdPx ?? 0;
 	const dragSession = controller.getDragSession();
 	const draggedPathSet = useMemo(() => dragSession == null ? null : new Set(dragSession.draggedPaths), [dragSession]);
 	const dragTarget = dragSession?.target ?? null;
@@ -704,9 +716,12 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 		const item = controller.getItem(targetPath);
 		if (item == null) return;
 		const anchorButton = getTriggerAnchorButton(targetPath);
+		// Pin scroll for every menu open. focusPath → scrollFocusedRowIntoView uses
+		// stickyOverlayHeight as topInset and jumps the scroller when the row sits in
+		// the sticky occlusion band (common in git review trees with stickyFolders).
+		const scrollElement = scrollRef.current;
+		preserveStickyKeyboardFocusAtScrollTop(targetPath, scrollElement?.scrollTop ?? null);
 		if (anchorButton?.dataset.fileTreeStickyRow === "true") {
-			const scrollElement = scrollRef.current;
-			preserveStickyKeyboardFocusAtScrollTop(targetPath, scrollElement?.scrollTop ?? null);
 			domFocusOwnerRef.current = true;
 			setActiveItemPath((previousPath) => previousPath === targetPath ? previousPath : targetPath);
 		}
@@ -1021,6 +1036,134 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 			document.addEventListener("touchcancel", handleActiveTouchCancel);
 		}, TOUCH_LONG_PRESS_DELAY);
 	};
+	const handleRowPointerDown = (event, row, targetPath) => {
+		if (event.pointerType === "touch" || event.button !== 0 || !(pointerDragThresholdPx > 0) || !dragAndDropEnabled) return;
+		const dragSource = event.currentTarget;
+		if (dragSource == null) return;
+		pointerCleanupRef.current?.();
+		pointerDragActiveRef.current = false;
+		pointerDragArmedRef.current = {
+			x: event.clientX,
+			y: event.clientY,
+			path: targetPath,
+			row
+		};
+		const threshold = pointerDragThresholdPx;
+		const clearPointerListeners = () => {
+			window.removeEventListener("pointermove", handlePointerMove);
+			window.removeEventListener("pointerup", handlePointerUp);
+			window.removeEventListener("pointercancel", handlePointerCancel);
+			window.removeEventListener("keydown", handlePointerKeyDown);
+			if (pointerCleanupRef.current === clearPointerListeners) pointerCleanupRef.current = null;
+		};
+		const disarmPointerDrag = () => {
+			clearPointerListeners();
+			pointerDragArmedRef.current = null;
+			pointerDragActiveRef.current = false;
+			pointerPreviewOffsetRef.current = null;
+		};
+		const armSuppressNextClick = (deferClearUntilPointerUp = false) => {
+			suppressClickRef.current = true;
+			let timeoutId = 0;
+			const swallowClick = (clickEvent) => {
+				clickEvent.preventDefault();
+				clickEvent.stopPropagation();
+				suppressClickRef.current = false;
+				window.removeEventListener("click", swallowClick, true);
+				if (timeoutId !== 0) {
+					clearTimeout(timeoutId);
+					timeoutId = 0;
+				}
+			};
+			const clearSuppressClick = () => {
+				suppressClickRef.current = false;
+				window.removeEventListener("click", swallowClick, true);
+			};
+			window.addEventListener("click", swallowClick, { capture: true, once: true });
+			if (deferClearUntilPointerUp) {
+				const scheduleClear = () => {
+					timeoutId = setTimeout(clearSuppressClick, 0);
+				};
+				window.addEventListener("pointerup", scheduleClear, { capture: true, once: true });
+				window.addEventListener("pointercancel", scheduleClear, { capture: true, once: true });
+			} else timeoutId = setTimeout(clearSuppressClick, 0);
+		};
+		const cancelArmedPointerDrag = (deferClickSuppress = false) => {
+			const wasActive = pointerDragActiveRef.current;
+			disarmPointerDrag();
+			controller.cancelDrag();
+			clearDragPreview();
+			clearDragHoverOpen();
+			stopDragAutoScroll();
+			dragRowSnapshotRef.current = null;
+			if (wasActive) {
+				suppressClickRef.current = true;
+				armSuppressNextClick(deferClickSuppress);
+			}
+		};
+		const handlePointerMove = (moveEvent) => {
+			const armed = pointerDragArmedRef.current;
+			if (armed == null) return;
+			const dx = moveEvent.clientX - armed.x;
+			const dy = moveEvent.clientY - armed.y;
+			if (!pointerDragActiveRef.current) {
+				if (dx * dx + dy * dy < threshold * threshold) return;
+				if (controller.startDrag(armed.path) === false) {
+					disarmPointerDrag();
+					return;
+				}
+				pointerDragActiveRef.current = true;
+				dragRowSnapshotRef.current = armed.row;
+				const rect = dragSource.getBoundingClientRect();
+				const preview = createDragPreviewElement(dragSource);
+				Object.assign(preview.style, {
+					height: `${rect.height}px`,
+					opacity: "0.85",
+					transform: `translate3d(${rect.left}px, ${rect.top}px, 0)`,
+					width: `${rect.width}px`
+				});
+				mountDragPreview(preview);
+				dragPreviewRef.current = preview;
+				pointerPreviewOffsetRef.current = {
+					x: armed.x - rect.left,
+					y: armed.y - rect.top
+				};
+			}
+			moveEvent.preventDefault();
+			const previewOffset = pointerPreviewOffsetRef.current;
+			if (previewOffset != null && dragPreviewRef.current != null) dragPreviewRef.current.style.transform = `translate3d(${moveEvent.clientX - previewOffset.x}px, ${moveEvent.clientY - previewOffset.y}px, 0)`;
+			scheduleDragHoverOpen(syncDropTargetFromPoint(moveEvent.clientX, moveEvent.clientY));
+			updateDragPoint(moveEvent.clientX, moveEvent.clientY);
+		};
+		const handlePointerUp = (upEvent) => {
+			const wasActive = pointerDragActiveRef.current;
+			if (!wasActive) {
+				disarmPointerDrag();
+				return;
+			}
+			syncDropTargetFromPoint(upEvent.clientX, upEvent.clientY);
+			controller.completeDrag();
+			suppressClickRef.current = true;
+			armSuppressNextClick();
+			disarmPointerDrag();
+			clearDragPreview();
+			clearDragHoverOpen();
+			stopDragAutoScroll();
+			dragRowSnapshotRef.current = null;
+		};
+		const handlePointerCancel = () => {
+			cancelArmedPointerDrag();
+		};
+		const handlePointerKeyDown = (keyEvent) => {
+			if (keyEvent.key !== "Escape") return;
+			cancelArmedPointerDrag(true);
+		};
+		window.addEventListener("pointermove", handlePointerMove, { passive: false });
+		window.addEventListener("pointerup", handlePointerUp);
+		window.addEventListener("pointercancel", handlePointerCancel);
+		window.addEventListener("keydown", handlePointerKeyDown);
+		pointerCleanupRef.current = clearPointerListeners;
+	};
 	const handleTreeKeyDown = (event) => {
 		if (contextMenuState != null) {
 			if (event.key === "Escape") {
@@ -1051,7 +1194,7 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 			event.stopPropagation();
 			return;
 		}
-		if (isSearchOpen) {
+		if (isSearchOpen && searchEnabled) {
 			if (event.key === "Escape") {
 				restoreTreeFocusAfterSearchCloseRef.current = false;
 				restoreTreeFocusViewportOffsetRef.current = null;
@@ -1493,7 +1636,11 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 			if (scrollRequestRow != null) {
 				const scrollRequestTopInset = stickyFolders ? Math.max(0, Math.min(scrollRequestRow.ancestorPaths.length * itemHeight, Math.max(0, resolvedViewportHeight - itemHeight))) : stickyOverlayHeight;
 				shouldSuppressDomFocusForScrollRequest = true;
-				shouldUpdateViewportForScrollRequest = scrollFocusedRowToOffset(scrollElement, scrollRequestIndex, itemHeight, resolvedViewportHeight, totalScrollableHeight, scrollRequest.offset, scrollRequestTopInset);
+				const write = () => {
+					shouldUpdateViewportForScrollRequest = scrollFocusedRowToOffset(scrollElement, scrollRequestIndex, itemHeight, resolvedViewportHeight, totalScrollableHeight, scrollRequest.offset, scrollRequestTopInset);
+				};
+				if (scrollRequest.performScroll != null) scrollRequest.performScroll(scrollElement, write);
+				else write();
 			}
 			controller.clearScrollRequest(scrollRequest.id);
 		}
@@ -1599,6 +1746,8 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 		window.addEventListener("dragend", handleWindowDragEnd);
 		return () => {
 			window.removeEventListener("dragend", handleWindowDragEnd);
+			pointerCleanupRef.current?.();
+			pointerCleanupRef.current = null;
 			clearTouchDragResources();
 			controller.cancelDrag();
 		};
@@ -1692,15 +1841,20 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 			setActiveItemPath((previousPath) => previousPath === actionTargetPath ? previousPath : actionTargetPath);
 			setLastContextMenuInteraction("focus");
 		}
-		if (shouldToggleDirectory) controller.toggleMountedDirectoryFromInput(actionTargetPath);
-		if (plan.closeSearch) controller.closeSearch();
+		if (shouldToggleDirectory) {
+			controller.toggleMountedDirectoryFromInput(actionTargetPath);
+			const toggledItem = controller.getItem(actionTargetPath);
+			if (clickedElement != null && toggledItem?.isDirectory() === true) clickedElement.setAttribute("aria-expanded", String(toggledItem.isExpanded()));
+		}
+		if (plan.closeSearch && searchEnabled) controller.closeSearch();
 		if (plan.revealCanonical) revealCanonicalRowAtStickyOffset(actionTargetPath, { targetOffset: "sticky-parents" });
 	}, [
 		controller,
 		isSearchOpen,
 		layoutSnapshot.visible.endIndex,
 		layoutSnapshot.visible.startIndex,
-		revealCanonicalRowAtStickyOffset
+		revealCanonicalRowAtStickyOffset,
+		searchEnabled
 	]);
 	const openMenuFromTrigger = () => {
 		if (isScrollingRef.current) return;
@@ -1733,11 +1887,13 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 		dragAndDropEnabled,
 		draggedPathSet,
 		dragTarget,
+		pointerDragThresholdPx,
 		gitLaneActive,
 		gitStatusByPath,
 		handleRowDragEnd,
 		handleRowDragStart,
 		handleRowTouchStart,
+		handleRowPointerDown,
 		ignoredGitDirectories,
 		ignoredInheritanceCache,
 		instanceId,
@@ -1751,6 +1907,7 @@ function FileTreeView({ composition, controller, gitStatusByPath, ignoredGitDire
 		renderDecorationForRow,
 		resolveIcon,
 		shouldSuppressContextMenu,
+		suppressClickRef,
 		visualFocusPath
 	};
 	const stickyRowFrame = {
