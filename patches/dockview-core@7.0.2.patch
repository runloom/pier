diff --git a/dist/cjs/dnd/dropTargetAnchorContainer.js b/dist/cjs/dnd/dropTargetAnchorContainer.js
index f98f8a50293348a6afad0b390bb1445b3d6f83fd..219a24354c2d6a47b1952d6f0c7503ea344094dd 100644
--- a/dist/cjs/dnd/dropTargetAnchorContainer.js
+++ b/dist/cjs/dnd/dropTargetAnchorContainer.js
@@ -24,8 +24,36 @@ var DropTargetAnchorContainer = /** @class */ (function (_super) {
         _this.element = element;
         _this._disabled = false;
         _this._disabled = options.disabled;
+        // Pier patch: `dndOverlayMounting: absolute` parks the overlay on this
+        // host, so Droptarget.onDragLeave no-ops (entering the overlay would
+        // otherwise flicker). HTML5 dragleave with relatedTarget outside the
+        // document is the real "pointer left this window" signal. In-window
+        // dragend often never reaches the content droptarget, so also clear
+        // when the HTML5 drag session ends.
+        var onDocumentDragLeave = function (e) {
+            var _a;
+            if (_this._disabled || !_this._model) {
+                return;
+            }
+            var related = e.relatedTarget;
+            if (related instanceof Node && document.documentElement.contains(related)) {
+                return;
+            }
+            (_a = _this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        var onDocumentDragEnd = function () {
+            var _a;
+            if (_this._disabled || !_this._model) {
+                return;
+            }
+            (_a = _this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        document.addEventListener('dragleave', onDocumentDragLeave, true);
+        document.addEventListener('dragend', onDocumentDragEnd, true);
         _this.addDisposables(lifecycle_1.Disposable.from(function () {
             var _a;
+            document.removeEventListener('dragleave', onDocumentDragLeave, true);
+            document.removeEventListener('dragend', onDocumentDragEnd, true);
             (_a = _this.model) === null || _a === void 0 ? void 0 : _a.clear();
         }));
         return _this;
diff --git a/dist/cjs/scrollbar.js b/dist/cjs/scrollbar.js
index c75388513414f854dfce14d15aebd50097c69a56..3ef237e22aaad16f21727956ed4b726e77abde7c 100644
--- a/dist/cjs/scrollbar.js
+++ b/dist/cjs/scrollbar.js
@@ -159,7 +159,8 @@ var Scrollbar = /** @class */ (function (_super) {
                 this._scrollbar.style.height = '0px';
                 this._scrollbar.style.top = '0px';
             }
-            this._scrollOffset = 0;
+            // Pier patch: preserve scroll offset while non-overflowing
+            // (e.g. group size→0 during maximize). Clamp resumes when overflow returns.
         }
     };
     Scrollbar.MouseWheelSpeed = 1;
diff --git a/dist/esm/dnd/dropTargetAnchorContainer.js b/dist/esm/dnd/dropTargetAnchorContainer.js
index f374a8c92baba5e16552a332954d90acdbdbffa6..bdef528a033a2451bcf3931d1915e321355fe5b8 100644
--- a/dist/esm/dnd/dropTargetAnchorContainer.js
+++ b/dist/esm/dnd/dropTargetAnchorContainer.js
@@ -55,8 +55,36 @@ export class DropTargetAnchorContainer extends CompositeDisposable {
         this.element = element;
         this._disabled = false;
         this._disabled = options.disabled;
+        // Pier patch: `dndOverlayMounting: absolute` parks the overlay on this
+        // host, so Droptarget.onDragLeave no-ops (entering the overlay would
+        // otherwise flicker). HTML5 dragleave with relatedTarget outside the
+        // document is the real "pointer left this window" signal. In-window
+        // dragend often never reaches the content droptarget, so also clear
+        // when the HTML5 drag session ends.
+        const onDocumentDragLeave = (e) => {
+            var _a;
+            if (this._disabled || !this._model) {
+                return;
+            }
+            const related = e.relatedTarget;
+            if (related instanceof Node && document.documentElement.contains(related)) {
+                return;
+            }
+            (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        const onDocumentDragEnd = () => {
+            var _a;
+            if (this._disabled || !this._model) {
+                return;
+            }
+            (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        document.addEventListener('dragleave', onDocumentDragLeave, true);
+        document.addEventListener('dragend', onDocumentDragEnd, true);
         this.addDisposables(Disposable.from(() => {
             var _a;
+            document.removeEventListener('dragleave', onDocumentDragLeave, true);
+            document.removeEventListener('dragend', onDocumentDragEnd, true);
             (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
         }));
     }
diff --git a/dist/esm/scrollbar.js b/dist/esm/scrollbar.js
index 3a1f999edd6826bb9d3c00d027c86145c2eda62f..1ea146a52c93bf715567cbaf4f1ac5c332c96417 100644
--- a/dist/esm/scrollbar.js
+++ b/dist/esm/scrollbar.js
@@ -131,7 +131,8 @@ export class Scrollbar extends CompositeDisposable {
                 this._scrollbar.style.height = '0px';
                 this._scrollbar.style.top = '0px';
             }
-            this._scrollOffset = 0;
+            // Pier patch: preserve scroll offset while non-overflowing
+            // (e.g. group size→0 during maximize). Clamp resumes when overflow returns.
         }
     }
 }
diff --git a/dist/package/main.cjs.js b/dist/package/main.cjs.js
index aa2c51060a1c2f31a87f2b43e57d81488a7f87f4..94f27f50e754d1eda1b1762b60516031cafeb1e5 100644
--- a/dist/package/main.cjs.js
+++ b/dist/package/main.cjs.js
@@ -6845,7 +6845,8 @@ class Scrollbar extends CompositeDisposable {
                 this._scrollbar.style.height = '0px';
                 this._scrollbar.style.top = '0px';
             }
-            this._scrollOffset = 0;
+            // Pier patch: preserve scroll offset while non-overflowing
+            // (e.g. group size→0 during maximize). Clamp resumes when overflow returns.
         }
     }
 }
@@ -14751,8 +14752,36 @@ class DropTargetAnchorContainer extends CompositeDisposable {
         this.element = element;
         this._disabled = false;
         this._disabled = options.disabled;
+        // Pier patch: `dndOverlayMounting: absolute` parks the overlay on this
+        // host, so Droptarget.onDragLeave no-ops (entering the overlay would
+        // otherwise flicker). HTML5 dragleave with relatedTarget outside the
+        // document is the real "pointer left this window" signal. In-window
+        // dragend often never reaches the content droptarget, so also clear
+        // when the HTML5 drag session ends.
+        const onDocumentDragLeave = (e) => {
+            var _a;
+            if (this._disabled || !this._model) {
+                return;
+            }
+            const related = e.relatedTarget;
+            if (related instanceof Node && document.documentElement.contains(related)) {
+                return;
+            }
+            (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        const onDocumentDragEnd = () => {
+            var _a;
+            if (this._disabled || !this._model) {
+                return;
+            }
+            (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        document.addEventListener('dragleave', onDocumentDragLeave, true);
+        document.addEventListener('dragend', onDocumentDragEnd, true);
         this.addDisposables(exports.DockviewDisposable.from(() => {
             var _a;
+            document.removeEventListener('dragleave', onDocumentDragLeave, true);
+            document.removeEventListener('dragend', onDocumentDragEnd, true);
             (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
         }));
     }
diff --git a/dist/package/main.esm.mjs b/dist/package/main.esm.mjs
index 13e69c289de428eb9129db015c36b67ae3d6018f..827be0e95b1fe69137d3a3d69dfad0bc6411a78b 100644
--- a/dist/package/main.esm.mjs
+++ b/dist/package/main.esm.mjs
@@ -6841,7 +6841,8 @@ class Scrollbar extends CompositeDisposable {
                 this._scrollbar.style.height = '0px';
                 this._scrollbar.style.top = '0px';
             }
-            this._scrollOffset = 0;
+            // Pier patch: preserve scroll offset while non-overflowing
+            // (e.g. group size→0 during maximize). Clamp resumes when overflow returns.
         }
     }
 }
@@ -14747,8 +14748,36 @@ class DropTargetAnchorContainer extends CompositeDisposable {
         this.element = element;
         this._disabled = false;
         this._disabled = options.disabled;
+        // Pier patch: `dndOverlayMounting: absolute` parks the overlay on this
+        // host, so Droptarget.onDragLeave no-ops (entering the overlay would
+        // otherwise flicker). HTML5 dragleave with relatedTarget outside the
+        // document is the real "pointer left this window" signal. In-window
+        // dragend often never reaches the content droptarget, so also clear
+        // when the HTML5 drag session ends.
+        const onDocumentDragLeave = (e) => {
+            var _a;
+            if (this._disabled || !this._model) {
+                return;
+            }
+            const related = e.relatedTarget;
+            if (related instanceof Node && document.documentElement.contains(related)) {
+                return;
+            }
+            (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        const onDocumentDragEnd = () => {
+            var _a;
+            if (this._disabled || !this._model) {
+                return;
+            }
+            (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
+        };
+        document.addEventListener('dragleave', onDocumentDragLeave, true);
+        document.addEventListener('dragend', onDocumentDragEnd, true);
         this.addDisposables(Disposable.from(() => {
             var _a;
+            document.removeEventListener('dragleave', onDocumentDragLeave, true);
+            document.removeEventListener('dragend', onDocumentDragEnd, true);
             (_a = this.model) === null || _a === void 0 ? void 0 : _a.clear();
         }));
     }
