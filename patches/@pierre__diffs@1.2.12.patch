diff --git a/dist/components/CodeView.d.ts b/dist/components/CodeView.d.ts
index a720beb585d03b5f49ab64a7ffab9274ff9e6a83..b92636c0b6ea126738b47cfe160bc3c32b8d9f45 100644
--- a/dist/components/CodeView.d.ts
+++ b/dist/components/CodeView.d.ts
@@ -73,7 +73,7 @@ type CodeViewOptionCallback<LAnnotation, TKey extends keyof FileOptions<LAnnotat
   (...args: [...OverloadFileCallbackArgs<LAnnotation, TKey>, context: CodeViewFileItemContext<LAnnotation>]): CallbackReturn<NonNullable<FileOptions<LAnnotation>[TKey]>>;
   (...args: [...OverloadDiffCallbackArgs<LAnnotation, TKey>, context: CodeViewDiffItemContext<LAnnotation>]): CallbackReturn<NonNullable<FileDiffOptions<LAnnotation>[TKey]>>;
 };
-declare const CODE_VIEW_DIFF_OPTION_KEYS: readonly ['theme', 'disableLineNumbers', 'overflow', 'themeType', 'disableFileHeader', 'disableVirtualizationBuffers', 'preferredHighlighter', 'useCSSClasses', 'useTokenTransformer', 'tokenizeMaxLineLength', 'tokenizeMaxLength', 'unsafeCSS', 'diffStyle', 'diffIndicators', 'disableBackground', 'expandUnchanged', 'collapsedContextThreshold', 'lineDiffType', 'maxLineDiffLength', 'expansionLineCount', 'lineHoverHighlight', 'enableTokenInteractionsOnWhitespace', 'enableGutterUtility', '__debugPointerEvents', 'enableLineSelection', 'controlledSelection', 'disableErrorHandling'];
+declare const CODE_VIEW_DIFF_OPTION_KEYS: readonly ['theme', 'disableLineNumbers', 'overflow', 'themeType', 'disableFileHeader', 'disableVirtualizationBuffers', 'preferredHighlighter', 'useCSSClasses', 'useTokenTransformer', 'tokenizeMaxLineLength', 'tokenizeMaxLength', 'unsafeCSS', 'diffStyle', 'diffIndicators', 'disableBackground', 'expandUnchanged', 'collapsedContextThreshold', 'lineDiffType', 'maxLineDiffLength', 'expansionLineCount', 'formatUnmodifiedLines', 'expandAllUnmodifiedLabel', 'lineHoverHighlight', 'enableTokenInteractionsOnWhitespace', 'enableGutterUtility', '__debugPointerEvents', 'enableLineSelection', 'controlledSelection', 'disableErrorHandling'];
 type CodeViewDiffOptionKeys = (typeof CODE_VIEW_DIFF_OPTION_KEYS)[number];
 type CodeViewPassThroughOptions<LAnnotation> = Pick<FileDiffOptions<LAnnotation>, CodeViewDiffOptionKeys>;
 declare const CODE_VIEW_SHARED_CALLBACK_KEYS: readonly ['renderCustomHeader', 'renderHeaderPrefix', 'renderHeaderMetadata', 'renderAnnotation', 'renderGutterUtility', 'onPostRender', 'onGutterUtilityClick', 'onLineClick', 'onLineNumberClick', 'onLineEnter', 'onLineLeave', 'onTokenClick', 'onTokenEnter', 'onTokenLeave'];
diff --git a/dist/components/CodeView.js b/dist/components/CodeView.js
index 3c51a218da6df6ec4b53522c0b07a95280473cea..7073c63b5518aa3b6244e7f0471052cc285de2ac 100644
--- a/dist/components/CodeView.js
+++ b/dist/components/CodeView.js
@@ -32,6 +32,8 @@ const CODE_VIEW_DIFF_OPTION_KEYS = [
 	"lineDiffType",
 	"maxLineDiffLength",
 	"expansionLineCount",
+	"formatUnmodifiedLines",
+	"expandAllUnmodifiedLabel",
 	"lineHoverHighlight",
 	"enableTokenInteractionsOnWhitespace",
 	"enableGutterUtility",
@@ -1646,7 +1648,7 @@ function shouldClearPool(previousOptions, nextOptions) {
 	return !areThemesEqual(previousOptions.theme ?? DEFAULT_THEMES, nextOptions.theme ?? DEFAULT_THEMES) || (previousOptions.themeType ?? "system") !== (nextOptions.themeType ?? "system") || previousOptions.unsafeCSS !== nextOptions.unsafeCSS;
 }
 function hasItemLayoutOptionChanged(previousOptions, nextOptions) {
-	return (previousOptions.overflow ?? "scroll") !== (nextOptions.overflow ?? "scroll") || (previousOptions.disableLineNumbers ?? false) !== (nextOptions.disableLineNumbers ?? false) || (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || previousOptions.unsafeCSS !== nextOptions.unsafeCSS || (previousOptions.diffStyle ?? "split") !== (nextOptions.diffStyle ?? "split") || (previousOptions.diffIndicators ?? "bars") !== (nextOptions.diffIndicators ?? "bars") || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1);
+	return (previousOptions.overflow ?? "scroll") !== (nextOptions.overflow ?? "scroll") || (previousOptions.disableLineNumbers ?? false) !== (nextOptions.disableLineNumbers ?? false) || (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || previousOptions.unsafeCSS !== nextOptions.unsafeCSS || (previousOptions.diffStyle ?? "split") !== (nextOptions.diffStyle ?? "split") || (previousOptions.diffIndicators ?? "bars") !== (nextOptions.diffIndicators ?? "bars") || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1) || previousOptions.formatUnmodifiedLines !== nextOptions.formatUnmodifiedLines || previousOptions.expandAllUnmodifiedLabel !== nextOptions.expandAllUnmodifiedLabel;
 }
 function hasCodeViewDiffEstimateOptionChanged(previousOptions, nextOptions) {
 	return (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1);
diff --git a/dist/components/VirtualizedFileDiff.js b/dist/components/VirtualizedFileDiff.js
index 3d48b53247ae3bd1f8b8c2b668efe084d723cdf5..655739ea84ca95b86e16e1329e7ff7b34228976e 100644
--- a/dist/components/VirtualizedFileDiff.js
+++ b/dist/components/VirtualizedFileDiff.js
@@ -813,7 +813,7 @@ function getHunkMetadataOffsets({ diffStyle, hunk, rowCount }) {
 	return offsets;
 }
 function hasDiffLayoutOptionChanged(previousOptions, nextOptions) {
-	return (previousOptions.diffStyle ?? "split") !== (nextOptions.diffStyle ?? "split") || (previousOptions.overflow ?? "scroll") !== (nextOptions.overflow ?? "scroll") || (previousOptions.collapsed ?? false) !== (nextOptions.collapsed ?? false) || (previousOptions.disableLineNumbers ?? false) !== (nextOptions.disableLineNumbers ?? false) || (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || (previousOptions.diffIndicators ?? "bars") !== (nextOptions.diffIndicators ?? "bars") || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1) || previousOptions.unsafeCSS !== nextOptions.unsafeCSS;
+	return (previousOptions.diffStyle ?? "split") !== (nextOptions.diffStyle ?? "split") || (previousOptions.overflow ?? "scroll") !== (nextOptions.overflow ?? "scroll") || (previousOptions.collapsed ?? false) !== (nextOptions.collapsed ?? false) || (previousOptions.disableLineNumbers ?? false) !== (nextOptions.disableLineNumbers ?? false) || (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || (previousOptions.diffIndicators ?? "bars") !== (nextOptions.diffIndicators ?? "bars") || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1) || previousOptions.unsafeCSS !== nextOptions.unsafeCSS || previousOptions.formatUnmodifiedLines !== nextOptions.formatUnmodifiedLines || previousOptions.expandAllUnmodifiedLabel !== nextOptions.expandAllUnmodifiedLabel;
 }
 function hasDiffEstimateOptionChanged(previousOptions, nextOptions) {
 	return (previousOptions.disableFileHeader ?? false) !== (nextOptions.disableFileHeader ?? false) || (previousOptions.hunkSeparators ?? "line-info") !== (nextOptions.hunkSeparators ?? "line-info") || (previousOptions.expandUnchanged ?? false) !== (nextOptions.expandUnchanged ?? false) || (previousOptions.collapsedContextThreshold ?? 1) !== (nextOptions.collapsedContextThreshold ?? 1);
diff --git a/dist/renderers/DiffHunksRenderer.js b/dist/renderers/DiffHunksRenderer.js
index d1a34789994239239a0e3ff751292ac34a21e44a..b0ece21f9e04f4934cf5bcbdc761f3125743d282 100644
--- a/dist/renderers/DiffHunksRenderer.js
+++ b/dist/renderers/DiffHunksRenderer.js
@@ -114,7 +114,7 @@ var DiffHunksRenderer = class {
 		return createAnnotationElement(span);
 	};
 	getOptionsWithDefaults() {
-		const { diffIndicators = "bars", diffStyle = "split", disableBackground = false, disableFileHeader = false, disableLineNumbers = false, disableVirtualizationBuffers = false, collapsed = false, expandUnchanged = false, collapsedContextThreshold = 1, expansionLineCount = 100, hunkSeparators = "line-info", lineDiffType = "word-alt", maxLineDiffLength = 1e3, overflow = "scroll", stickyHeader = false, theme = DEFAULT_THEMES, headerRenderMode = "default", tokenizeMaxLineLength = 1e3, tokenizeMaxLength = DEFAULT_TOKENIZE_MAX_LENGTH, useTokenTransformer = false, useCSSClasses = false } = this.options;
+		const { diffIndicators = "bars", diffStyle = "split", disableBackground = false, disableFileHeader = false, disableLineNumbers = false, disableVirtualizationBuffers = false, collapsed = false, expandUnchanged = false, collapsedContextThreshold = 1, expansionLineCount = 100, hunkSeparators = "line-info", lineDiffType = "word-alt", maxLineDiffLength = 1e3, overflow = "scroll", stickyHeader = false, theme = DEFAULT_THEMES, headerRenderMode = "default", tokenizeMaxLineLength = 1e3, tokenizeMaxLength = DEFAULT_TOKENIZE_MAX_LENGTH, useTokenTransformer = false, useCSSClasses = false, formatUnmodifiedLines, expandAllUnmodifiedLabel } = this.options;
 		return {
 			diffIndicators,
 			diffStyle,
@@ -136,7 +136,9 @@ var DiffHunksRenderer = class {
 			tokenizeMaxLineLength,
 			tokenizeMaxLength,
 			useTokenTransformer,
-			useCSSClasses
+			useCSSClasses,
+			formatUnmodifiedLines,
+			expandAllUnmodifiedLabel
 		};
 	}
 	async initializeHighlighter() {
@@ -312,7 +314,7 @@ var DiffHunksRenderer = class {
 		return this.options.tokenizeMaxLength ?? 1e5;
 	}
 	processDiffResult(fileDiff, renderRange, { code, themeStyles, baseThemeType }) {
-		const { diffStyle, disableFileHeader, expandUnchanged, expansionLineCount, collapsedContextThreshold, hunkSeparators } = this.getOptionsWithDefaults();
+		const { diffStyle, disableFileHeader, expandUnchanged, expansionLineCount, collapsedContextThreshold, hunkSeparators, formatUnmodifiedLines, expandAllUnmodifiedLabel } = this.getOptionsWithDefaults();
 		this.diff = fileDiff;
 		const unified = diffStyle === "unified";
 		let additionsContentAST = [];
@@ -323,6 +325,8 @@ var DiffHunksRenderer = class {
 		const context = {
 			rowCount: 0,
 			hunkSeparators,
+			formatUnmodifiedLines,
+			expandAllUnmodifiedLabel,
 			additionsContentAST,
 			deletionsContentAST,
 			unifiedContentAST,
@@ -726,7 +730,8 @@ function getAnnotationNames(annotations) {
 	return annotations?.map((annotation) => getLineAnnotationName(annotation)) ?? [];
 }
 const EN_PLURAL_RULES = new Intl.PluralRules("en-US");
-function getModifiedLinesString(lines) {
+function getModifiedLinesString(lines, formatUnmodifiedLines) {
+	if (typeof formatUnmodifiedLines === "function") return formatUnmodifiedLines(lines);
 	return `${lines} unmodified line${EN_PLURAL_RULES.select(lines) === "one" ? "" : "s"}`;
 }
 function pushUnifiedInjectedRows(rows, context) {
@@ -825,23 +830,26 @@ function pushSeparator(type, { hunkIndex, collapsedLines, rangeSize, hunkSpecs,
 	const slotName = getHunkSeparatorSlotName(type, hunkIndex);
 	const chunked = rangeSize > context.expansionLineCount;
 	const expandIndex = isExpandable ? hunkIndex : void 0;
+	const unmodifiedContent = getModifiedLinesString(collapsedLines, context.formatUnmodifiedLines);
 	context.pushToGutter(type, createSeparator({
 		type: context.hunkSeparators,
-		content: getModifiedLinesString(collapsedLines),
+		content: unmodifiedContent,
 		expandIndex,
 		chunked,
 		slotName,
 		isFirstHunk,
-		isLastHunk
+		isLastHunk,
+		expandAllLabel: context.expandAllUnmodifiedLabel
 	}));
 	linesAST.push(createSeparator({
 		type: context.hunkSeparators,
-		content: getModifiedLinesString(collapsedLines),
+		content: unmodifiedContent,
 		expandIndex,
 		chunked,
 		slotName,
 		isFirstHunk,
-		isLastHunk
+		isLastHunk,
+		expandAllLabel: context.expandAllUnmodifiedLabel
 	}));
 	if (type !== "additions") context.incrementRowCount(1);
 	context.hunkData.push({
diff --git a/dist/types.d.ts b/dist/types.d.ts
index d2fb6453a48cad580cd48c0bff49497cc1f8d7ea..68f3825bffa4f68cb4b555dbd983ba579d18468d 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -315,8 +315,19 @@ interface BaseDiffOptions extends BaseCodeOptions {
    * from file contents (oldFile/newFile). Has no effect on pre-parsed patches.
    */
   parseDiffOptions?: CreatePatchOptionsNonabortable;
+  /**
+   * Localized label for collapsed unmodified context rows.
+   * Receives the collapsed line count. Defaults to English:
+   * `${n} unmodified line(s)`.
+   */
+  formatUnmodifiedLines?: (lines: number) => string;
+  /**
+   * Label for the chunked separator "Expand all" control.
+   * Defaults to English "Expand all".
+   */
+  expandAllUnmodifiedLabel?: string;
 }
-type BaseDiffOptionsWithDefaults = Required<Omit<BaseDiffOptions, 'unsafeCSS' | 'preferredHighlighter' | 'parseDiffOptions'>>;
+type BaseDiffOptionsWithDefaults = Required<Omit<BaseDiffOptions, 'unsafeCSS' | 'preferredHighlighter' | 'parseDiffOptions' | 'formatUnmodifiedLines' | 'expandAllUnmodifiedLabel'>>;
 type CustomPreProperties = Record<string, string | number | undefined>;
 interface PrePropertiesConfig extends Required<Pick<BaseDiffOptions, 'diffIndicators' | 'disableBackground' | 'disableLineNumbers' | 'overflow'>> {
   type: 'diff' | 'file';
diff --git a/dist/utils/createSeparator.d.ts b/dist/utils/createSeparator.d.ts
index 11e9e3f192539daf6025a0f1a6500710ee7137c7..bb6a57c61a9ba03d994a04449a43d574c02d9eb0 100644
--- a/dist/utils/createSeparator.d.ts
+++ b/dist/utils/createSeparator.d.ts
@@ -10,6 +10,8 @@ interface CreateSeparatorProps {
   slotName?: string;
   isFirstHunk: boolean;
   isLastHunk: boolean;
+  /** Localized label for the chunked "Expand all" control. Defaults to English. */
+  expandAllLabel?: string;
 }
 declare function createSeparator({
   type,
@@ -18,7 +20,8 @@ declare function createSeparator({
   chunked,
   slotName,
   isFirstHunk,
-  isLastHunk
+  isLastHunk,
+  expandAllLabel
 }: CreateSeparatorProps): Element;
 //#endregion
 export { createSeparator };
diff --git a/dist/utils/createSeparator.js b/dist/utils/createSeparator.js
index e13a442d292fbd370ba474731f6fae8272c1b2b4..84bb6ea938f277987255c6d7a3d918815dbd86f4 100644
--- a/dist/utils/createSeparator.js
+++ b/dist/utils/createSeparator.js
@@ -16,7 +16,7 @@ function createExpandButton(type) {
 		}
 	});
 }
-function createSeparator({ type, content, expandIndex, chunked = false, slotName, isFirstHunk, isLastHunk }) {
+function createSeparator({ type, content, expandIndex, chunked = false, slotName, isFirstHunk, isLastHunk, expandAllLabel }) {
 	let buttonCount = 0;
 	const children = [];
 	if (type === "metadata" && content != null) children.push(createHastElement({
@@ -50,7 +50,7 @@ function createSeparator({ type, content, expandIndex, chunked = false, slotName
 		}));
 		if (chunked && expandIndex != null) contentChildren.push(createHastElement({
 			tagName: "div",
-			children: [createTextNodeElement("Expand all")],
+			children: [createTextNodeElement(expandAllLabel ?? "Expand all")],
 			properties: {
 				role: "button",
 				"data-expand-button": "",
diff --git a/dist/utils/getDiffHunksRendererOptions.js b/dist/utils/getDiffHunksRendererOptions.js
index 1684fa0808954076be40a3ebde3867aae8c015a7..d2d01739352dbf6d0d59b8d4981870cdaad7535d 100644
--- a/dist/utils/getDiffHunksRendererOptions.js
+++ b/dist/utils/getDiffHunksRendererOptions.js
@@ -22,6 +22,8 @@ function getDiffHunksRendererOptions(options) {
 		lineDiffType: options?.lineDiffType,
 		maxLineDiffLength: options?.maxLineDiffLength,
 		expansionLineCount: options?.expansionLineCount,
+		formatUnmodifiedLines: options?.formatUnmodifiedLines,
+		expandAllUnmodifiedLabel: options?.expandAllUnmodifiedLabel,
 		headerRenderMode: options?.renderCustomHeader != null ? "custom" : "default"
 	};
 }
