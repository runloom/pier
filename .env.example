# Pier 环境变量 (dev 本地拷贝为 .env.local, .env.local 在 .gitignore 内)

# === macOS notarize (release 用) ===
APPLE_API_KEY_ID=
APPLE_API_KEY_ISSUER=
APPLE_API_KEY_CONTENT=

# === Windows code sign (release 用) ===
WIN_CSC_LINK=
WIN_CSC_KEY_PASSWORD=

# === GitHub release ===
GH_TOKEN=
