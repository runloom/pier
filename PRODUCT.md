# Product

<!-- impeccable:product-schema 1 -->

## Scope

本文件是 Pier 的共同产品定义，适用于桌面端与所有移动端。用户已确认此范围；当前移动 Web / PWA，以及后续 iOS / Android App、小程序等客户端，都继承同一份用户目标、产品定位、术语与能力边界。会合服务和插件也须服务这些目标。

各端的专项文档可以补充设备能力、操作方式与交付阶段，不另建相互竞争的产品定义。架构、交互与工程治理继续以 [AGENTS.md](AGENTS.md) 为准；具体实现与已交付范围应核对源码和发布记录。

## Platform

web

## Users

主要用户是在项目目录或 git 工作树中，使用 Shell、Claude Code、Codex、OpenCode 等命令行工具开发软件的人。他们已有自己的命令行、账号、订阅和仓库，可能同时运行多个智能体会话，需要及时识别哪个会话正在运行、需要处理或出现错误。

桌面端服务持续的开发工作：输入命令、处理智能体请求、编辑文件、审查与提交变更。移动端服务同一用户离开电脑后的短时查看与处理：找到正在工作的电脑和会话，读懂当前情况，在授权范围内响应，再继续自己的活动。

## Product Purpose

Pier 是本地 AI 开发工作台。它把稳定终端、会话状态、文件编辑和 git 变更放在一个可持续使用的工作环境中，并通过移动端让用户离开电脑后仍能关注同一份工作。

成功意味着：用户能看清谁需要自己，准确回到对应的电脑、目录和会话，理解操作后果并完成处理；切换工具、窗口或设备时，正在运行的工作仍有明确的来源和可继续操作的位置。

## Positioning

Pier 以用户原有的命令行工作流为中心。智能体继续在自己的终端中运行，文件与 git 仍属于原仓库；Pier 提供宿主才能提供的终端承载、布局、状态观察、定位和配对访问能力。

各端围绕桌面宿主上的同一份工作展开。移动端读取宿主状态并向宿主提交获准的操作；会合服务帮助设备连接，代码执行仍由用户的开发环境承担。离开 Pier 后，原有命令行、账号和仓库仍可继续使用。

## Operating Context

当前可核实的界面实现是 Electron 中的 React 桌面界面与独立的移动 Web / PWA，因此在 Impeccable 中记录为 `web`。原生终端能力由桌面宿主提供。后续移动客户端继续继承本文件，并在各端专项文档中补充平台要求。

| 端或服务 | 职责 | 当前范围 |
| --- | --- | --- |
| 桌面端 | 承载终端、智能体与开发工具，管理文件、git、布局和远程设备授权 | 当前发布支持 macOS Apple Silicon / Intel；界面入口在 `src/renderer` |
| 移动 Web / PWA | 连接已配对电脑，查看会话、终端当前屏幕、文件、变更和消息，响应可处理的等待 | 实现在 `apps/mobile-web`；按宿主授权开放操作 |
| 后续移动 App、小程序 | 延续相同用户目标、宿主事实来源、配对身份和操作语义 | 共享协议已预留 `app` / `miniprogram` 类型；尚不能据此宣称客户端已经交付 |
| 会合服务 | 帮助已配对设备跨网连接桌面宿主 | 实现在 `apps/relay`；无账号、零持久化，盲转发加密数据 |

桌面典型流程：打开项目或工作树 → 启动命令行或智能体 → 查看状态并处理等待 → 查看文件与 diff → 暂存、提交或继续工作。标签、分屏、浮动面板和保存的布局用于组织这些工具。

移动典型流程：在桌面启用远程访问并配对设备 → 进入已配对电脑 → 打开已有会话 → 查看终端、只读文件与变更 → 在有效等待请求上发送所需按键。消息入口可带用户回到对应会话，文件与变更页保留返回来源。

远程访问依赖桌面宿主运行、电脑保持唤醒且可连接。配对后保存设备身份以便重连，宿主可撤销设备权限。移动推送依赖客户端能力、用户授权及订阅状态；连接失败、权限撤销与内容过期需要让用户分辨。

## Capabilities and Constraints

- **原有工具优先。** 宿主只实现依赖 Pier 身份或运行环境的独有能力。智能体已有的原生单次执行、分工等入口直接复用，避免重复包装成另一套宿主服务。
- **执行与状态有明确来源。** 终端、智能体活动、文件和 git 数据由现有宿主服务提供。各端共用契约与操作语义；移动操作仍由宿主按客户端类型及设备权限校验。
- **桌面支持完整的日常开发操作。** 包括稳定终端、多会话状态、文件查看与编辑、Markdown 与图片预览、git 审查和提交，以及工作树与布局管理。终端可在界面重新加载后继续使用，不能将此扩大为所有进程故障下的无损恢复承诺。
- **移动能力以实际读写范围为准。** 当前提供终端当前屏幕的纯文本读取、只读文件与 git diff、消息及已读操作。等待响应是受校验的按键发送，不能把 Enter / Esc 等按键擅自解释成已核实的“批准 / 拒绝”。当前 Web 界面不提供任意自由输入或新建终端、智能体、工作树。
- **权限和现有工作须受保护。** 配对与设备撤销由宿主管理；旧的等待请求不能继续操作。跨端动作应说明目标及影响，不能以界面上的成功状态替代宿主确认。
- **插件范围保持清楚。** 当前只接受内置插件和官方受管理外部插件。现有插件体系是可信代码的工程边界；第三方来源和真正隔离需要另行设计，不能作为现有能力宣传。
- **产品边界保持清楚。** 宿主不建立任务生命周期、SQLite 任务台账、看板或自动调度。Canvas 已能承载随项目保存的页面；以终端和状态组合看板、运行图等能力，须按各项实际交付情况说明。
- **数据按职责保存。** 用户偏好与布局主要保存在本地 userData JSON；文件和变更以磁盘与 git 为准，密钥使用 safeStorage。已明确规定的阅读、视口等本地偏好遵循其专项规则，各端不另造业务台账。

尚未确定或尚未完成的事项：原生移动 App、小程序的具体实现、发布与分发安排；移动端完整终端历史、彩色实时终端和自由输入的交付范围；移动端四语覆盖与各设备上的完整可用性验收。已有协议预留、设计稿和计划不能替代完成证明。

## Brand Commitments

产品名称为 **Pier**；移动端统一称 **Pier 移动端**。面向用户的表述围绕要做的事、操作对象、结果和下一步，不要求用户理解内部实现。

中文产品词统一使用“智能体”“工作树”“需要你处理”；Canvas 发现面称“物料”。git 功能名称、设置标题和命令分组使用“GIT”，正文说明使用“git”，GitHub 等专有名称保持原样。新文案遵循 [AGENTS.md](AGENTS.md) 的四语与可读性要求。

名称、标识与现有品牌资产的使用遵循 [TRADEMARKS.md](TRADEMARKS.md)。应用图标母版是 [build/app-icon-source.svg](build/app-icon-source.svg)。

## Evidence on Hand

- [README.md](README.md) 与并列的 [英文](README.en.md)、[日文](README.ja.md)、[韩文](README.ko.md)版本：产品定位、桌面工作流、当前发布平台与安装入口。
- [AGENTS.md](AGENTS.md) 与 [docs/README.md](docs/README.md)：产品边界、工程与交互规则，以及现行文档入口。
- [移动端路由](apps/mobile-web/src/lib/routes.ts)、[会话页](apps/mobile-web/src/pages/session.tsx)、[终端读屏](apps/mobile-web/src/components/terminal-screen.tsx)：当前移动端页面、会话定位与可操作范围的实现证据。
- [远程契约](src/shared/contracts/remote.ts)、[会合服务说明](apps/relay/README.md)、[发布指南](docs/release.md)：客户端类型、配对机制、会合服务职责与各部分发布方式。
- [官方插件说明](docs/plugins.md)：现有来源范围、加载流程与信任模型。
- [移动端方案](docs/superpowers/specs/2026-08-26-mobile-companion-design.md)、[移动端视觉与触控方案](docs/superpowers/specs/2026-09-01-mobile-web-visual-language.md)、[工作台体验提案](docs/superpowers/specs/2026-09-03-workbench-ux-and-ergonomics-design.md)：均带有待评审、待实施或待验证内容，使用时必须区分目标和当前实现。

本次初始化没有用户访谈、可用性测试报告或经验证的效率指标；不据此编写客户背书、性能倍数、全面平台支持或无障碍达标声明。

## Product Principles

1. **一份工作，多端继续。** 各端共享用户目标、对象身份和事实来源；操作方式适应设备，含义保持一致。
2. **让用户及时找到需要处理的事。** 状态必须对应真实的会话与请求，入口能回到可以继续工作的地方。
3. **保持原有开发工作流。** 原生命令行与仓库继续由用户掌握，Pier 补足宿主能力。
4. **每次操作都有可识别的结果。** 自然界面变化、即时反馈与后台消息按各自职责使用；避免静默失败和重复打扰。
5. **核心内聚，扩展有边界。** 终端、定位、状态和持续工作优先；领域工具通过既有插件能力扩展，遵守共同规则。

## Accessibility & Inclusion

桌面端须保留键盘操作和清楚的焦点指示，展示内容不为装饰效果进入 Tab 序。移动端须适应触控、窄屏与系统安全区，重要操作不能依赖 hover；具体命中范围和动效要求由各端规范承接，尊重减少动态效果的系统偏好。

全产品语言集合为 `zh-CN`、`en`、`ja`、`ko`。桌面已有四语 locale；移动端源码仍有待迁移的内联中文，因此四语是共同交付要求，不能描述为所有移动端已经完成。

状态与错误须有可读文字并给出下一步，操作控件须有可理解的名称。终端和文档的阅读偏好保留各自职责，适配多语言文本与用户的阅读需要。具体无障碍符合性和辅助技术兼容性仍需按端验证。
