# 工作台侧栏设计检查表

2026-09-09。依据工作树区域权威规格及用户随后明确的范围：仅侧栏；通高；任务等工作区目的地在顶部；设置、通知、更新在底部。用户首要目标是快速找到需要处理的会话。

唯一设计入口为 `.pier/canvases/workbench-sidebar-review/`，直接在 Pier Canvas 中打开。独立网页预览和导出代码已移除，交付边界见 [Canvas说明](../../../.pier/canvases/workbench-sidebar-review/README.md)。

## 本轮设计

- [x] Canvas直接渲染可编辑、可交互的侧栏；全部设计依赖在Canvas目录内，无截图嵌入，无完整宿主Sidebar引用。
- [x] 顶部唯一任务入口；中间项目树独立滚动；底部固定设置、通知、更新；打开项目位于项目标题旁。
- [x] 项目 → 工作树 → 会话按12px递进；项目无身份色，工作树色块与名称并置，会话使用真实品牌/终端图标几何。
- [x] 单行控件28px，主图标槽16px，状态点同列。项目加号独立28px命中区，在hover/focus出现。
- [x] 256px、224px、320px响应规则；名称优先、主目录标记完整；完整路径与摘要通过hover/focus可读。
- [x] 需要你处理的会话优先；完成未查看仍只以字重标识；项目不按状态跳动排序。
- [x] GIT摘要不把无法统计行数伪造成零；仅文件数样本在提示中说明原因。侧栏无分支或同步数。
- [x] 工作树选择、会话未读、项目折叠及方向键交互；任务和工作树分别暴露可访问选中态。
- [x] 更新下载前、42%下载中、下载完成三态；进度和完成标记在关闭浮层后仍可见。
- [x] 通知使用明确的示例空态；项目、会话、GIT数据、远程主机及更新进度均为示例。
- [x] 项目令牌、公开Canvas基础控件、四语文案；深浅主题与中英文窄栏实图经过独立复核。
- [x] 真实Live Modules默认围栏编译与公开SDK类型检查通过；外部预览已移除。

## 生产接线仍待实施

以下不是设计稿能证明的功能，不标记完成：

- [ ] 主窗按工作树整体切换区域，并保留终端、文件、草稿、后台进程和布局；已有区域恢复而非重复创建。
- [ ] 真实工作树、窗口面板清单、普通终端与智能体状态投影同源；状态、名称、主仓库分组键及跨窗显示名遵循现有契约。
- [ ] 会话定位仅在实际查看后更新真实已读状态；跨窗动作正确定位目标窗口。
- [ ] 任务等插件入口打开已有面板；设置、NCS、更新入口接入真实宿主服务。
- [ ] 异步加载、失败、重试及关闭窗口边界验证；通过适用治理与生产集成测试。

`AgentRuntimeIndex`仅含智能体，不能作为全量终端列表；`PanelContext.projectRootPath`也不能直接充当主仓库分组键。生产实施应使用权威规格里的身份与状态来源。

本轮没有终端、编辑器、任务正文、区域状态栏或草稿交互设计。前轮工作区样稿和相关测试仅为历史记录，不作为本轮侧栏设计的验收证据。
