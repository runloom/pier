# 智能体状态：周期审查任务模板

状态：任务内容已准备，**尚未在定时任务系统中创建或启用**。

## 已确认配置

| 项目 | 值 |
|---|---|
| 名称 | Pier 智能体状态上游审查 |
| 工作目录 | `/Users/xyz/ABC/pier` |
| 频率 | 每周一 10:00 |
| 时区 | `Asia/Shanghai` |
| 周期表达式 | `FREQ=WEEKLY;BYDAY=MO;BYHOUR=10;BYMINUTE=0;BYSECOND=0`；时区单独配置 |
| 深查周期 | 每月第一次运行；其余运行重点比较上游增量 |
| 首次基线 | [2026-09-06 审查](../specs/2026-09-06-agent-status-industry-audit.md) |
| 产物 | 一份有固定源码依据、可复现步骤和最小优化方案的中文报告 |

用户已确认按此方案开始实施，沿用每周一 10:00（Asia/Shanghai）的建议配置。确认配置不等于平台创建成功；目前没有任务 ID 或下一次执行时间的读回结果。

执行环境必须能读取项目代码并访问公开上游源码。仅在云端配置一个本机绝对路径，并不能获得该工作区：若环境只能访问 GitHub，应注明审查的是远端 commit，不能声称验证了用户未提交的修改。无法访问项目时只能完成上游变化摘要，并明确本地核验缺失。

本次会话没有可调用的定时任务管理工具，原生应用控制入口也未成功初始化，因此未创建定时任务。官方 [Automations 文档](https://learn.chatgpt.com/docs/automations?surface=app) 说明 CLI/IDE 可以准备和测试任务提示词，管理定时运行需使用支持的网页或桌面入口。后续应先确认所选入口及执行环境能访问目标代码，再使用本模板配置。核验结果、阻碍与 Pier 独立功能边界见 [接入范围](2026-09-06-pier-scheduled-review-scope.md)。

## 可直接使用的任务提示词

```text
请定期审查 Pier 的智能体状态实现，比较真实上游源码并提出优化方案。

项目：/Users/xyz/ABC/pier。使用简洁、通俗的中文回复。
这是研究与审查任务：允许读取代码、公开源码、版本信息，以及在临时目录执行隔离复现；允许写本次审查报告。不要自动修改产品代码、安装/升级 CLI、提交或推送 git，也不要向正在运行的智能体发送任务或中断信号。

每次先读：
1. AGENTS.md 中架构边界、前台活动、证据与回合归属规则。
2. docs/superpowers/specs/2026-09-06-agent-status-industry-audit.md。
3. 最近一次同类审查报告与现有状态证据矩阵、跨层轨迹测试。

先确认基线：
- 记录审查时间、时区、工作区 HEAD、dirty 状态和本次实际比较的文件范围。
- 对可用 CLI 只执行 --version 等只读探测；记录安装版本及仓库/发行渠道，不升级。
- 将本机已安装发行版与上游 HEAD 分开。开源项目优先解析发行 tag 对应 commit；无法精确匹配须说明。
- Kimi 区分 MoonshotAI/kimi-code 与旧 MoonshotAI/kimi-cli；不要因为名称相同就混用版本契约。
- 不读取或输出密钥、私人会话正文，不上传本地会话内容给搜索服务。

比较对象：
- stablyai/orca：状态映射、主/子身份、权限闭环、compact、恢复与过期事件。
- asheshgoplani/agent-deck：多界面统一推导、来源新鲜度、监听/连接恢复。
- coder/agentapi：终端观察与 stable/working 判定边界。
- Claude Code 官方 hooks 文档；Codex、Kimi、OpenCode 的官方文档和对应发行版本源码。
- 其他 Pier 已集成提供方按风险轮换；出现相关上游变更、用户故障或缺口时优先。

每周增量检查：
- 比较上次记录的 commit/tag 与本次来源，定位具体改动文件和行为。
- 检查新增/更名事件、载荷 ID、Stop 续跑、取消、权限、子智能体、会话记录格式、压缩和会话恢复变化。
- 上游没有变化也要核对 Pier 自身状态相关变更，特别是之前未关闭的问题。
- 源码不可访问时区分“无法核验”和“没有变化”，不要把旧结论当新证据。

每月首周深查：
- 重新走输入 → 适配器 → 回合/身份 → 归约 → 对账 → 快照/广播 → 界面消费链路。
- 优先复核 2026-09-06 报告 F1–F5 是否已修复且有独立验收。
- 轮换其余提供方，列明实际覆盖和未覆盖；不要把“共享测试通过”写成“全部提供方无问题”。
- 区分当前活动、上一回合结果、观察健康度；等待必须有结束闭环，子事件不能结算父回合，旧消息不能盖过新会话。
- 检查晚创建/替换/截断文件、读失败恢复、监听上限、窗口迁移、迟到/重复事件。
- 不因矩阵标为 unsupported 就免除上游已支持的正常路径；测试期望要由原生行为证明。

证据与复现：
- 技术结论只依据官方文档、协议或实际源码；README/宣传不能代替实现。
- 每项引用固定 commit 的源码链接，注明文件和关键位置；本地引用当前相对路径。
- A=实际 Pier 代码隔离复现，B=源码/官方契约证据，C=待验证推测；不要将 B/C 宣称为实机故障。
- 优先调用实际生成的钩子/插件及现有轨迹驱动；只给内部归约器喂人工事件不足以证明生产输入可达。
- 故障探针放在独立临时目录并清理；记录输入、期望、实际和阳性对照。不改用户 CLI 配置，不付费运行真实模型任务。
- 已满足本项验证后停止重复跑同一套检查；不因测试全绿就忽略错误的期望或缺失场景。

输出：
- 在 docs/superpowers/specs/YYYY-MM-DD-agent-status-industry-audit.md 写本次报告；同日已有报告时使用新后缀，保留历史。
- 开头用不超过 5 条说明本次新增/变化的结论；无可执行新建议时明确写没有，不凑问题。
- 每项包含：优先级、用户可见影响、触发条件、证据等级、本地位置、上游版本/固定链接、最小优化方案、验收用例。
- 对上次问题标记：仍存在 / 已复现修复 / 尚未复核，不能只因上游修了就标 Pier 已修。
- 给出分批实施顺序，复用现有适配器、归约器、广播和诊断，不增加公共 Transcript 服务、任务台账或第二套状态源。
- 附本次来源版本表、覆盖范围、未验证限制和下次重点，使下一轮只需比较增量。
- 最终回复先说有没有新增问题和最优先行动，再给报告链接。不要宣称自动修改已完成，也不要发送邮件或外部消息。
```

## 首次接入与验收

1. 用上面的提示词在有项目访问权限的执行环境手动运行一次，检查能读本地代码、查公开源码、写报告；本轮的首份审查可作内容基线，但不证明将来的定时环境也可访问项目。
2. 在可用的任务管理入口创建定时运行，填写时区和周期；检查返回的任务标识、启用状态、提示词、项目访问范围及下一次运行时间。
3. 只有读回上述配置且确认创建成功后，才能告知用户“已启用”。没有管理入口时保留本文件为待配置模板，不写入私有任务数据库，不用系统 cron 冒充平台任务。
4. 首次自动运行后核查报告有版本、固定源码链接及本地证据；若只拿到上游信息，输出范围必须相应缩小。

该任务只负责持续发现变化和提出方案。实施产品修复仍作为单独开发任务，避免周期审查不断扩大改动范围。
